{
  "$schema_comment": "Hooks that automate verification. The harness executes them, not the agent, so they cannot be skipped.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "npm test -- --reporter=dot 2>&1 | tail -5",
            "description": "After editing or writing files, run tests and show summary."
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "./init.sh > /tmp/harness_init.log 2>&1 && echo '[harness] init.sh OK' || (echo '[harness] init.sh FAILED — check /tmp/harness_init.log before closing' && cat /tmp/harness_init.log | tail -20)",
            "description": "Before closing the session, force a full verification."
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": ["Bash(./init.sh)", "Bash(npm test*)", "Bash(npx tsx src/cli.ts*)"]
  }
}
